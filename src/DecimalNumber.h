#include <map>
#include <string>
#include <vector>
inline std::map<int, char>conversorToRoman;
void loadConversionMapToRoman();
std::vector<int>transfrom(int n,int multiplier);
std::string vectorToString(std::vector<int>&nums);
std::string toRoman(int decimal);
